> **升级提醒**：从本版本起，任务进度按 PVP / PVPS / PVE 三套分开保存。
> 升级前已有的进度会自动归入 **PVP** 模式，收藏家进度同理；切换到 PVPS / PVE 时进度为空属于正常现象，并非数据丢失。

## 新增功能

- **任务页双视图**：任务列表与任务链图谱分开，图谱可自由平移缩放，任务卡片与节点双向跳转
- **任务进度三套独立存储**：PVP / PVPS / PVE 各存一套进度，互不影响；手机端自动跟随电脑端当前模式
- **转生（Prestige）任务标记**：转生门槛任务在图谱与列表中单独标识
- **手动完成 / 重置任务**：任务卡片与地图弹窗里都可直接标记完成或撤销
- **图谱详情面板**：可自由拖动并记住位置；新增「详情跟随」，面板贴在任务卡片下方并随画布一起移动
- **任务板筛选**：新增地图筛选（与搜索同一栏）与「显示已完成」开关，默认过滤已完成任务
- **地图绘制转移点**：标注地图间转移点的位置与「转移至 XX」目的地，弹窗显示目标地图
- **地图图标按楼层分级**：当前楼层正常显示，其它楼层压暗，并在图标右下角标出相对楼层方向（上一层朝上、下一层朝下，超过一层叠两个）
- **收藏家物品排序**：支持按物品类型与「未完成优先」排序

## 优化与修复

- **数据更新更省流量**：改为比对 ETag 的条件请求，内容未变的 JSON 直接跳过下载；更新过程显示「已更新 N/M 份文件」，设置页额外显示上次核对时间
- **商人名称统一用中文**，任务卡片不再重复渲染商人名与地点
- **出生点分类更清晰**：普通 AI 与狙击 AI 严格互斥，重命名为「普通 AI 出生点 / 狙击 AI 出生点」
- **地图楼层排序修正**：优先按层号排序，中文楼层名改用中文数字（一楼、二楼…）
- **任务区域黄色区块**：呼吸动画更慢、整体更透明，减少对读图的干扰
- **地图图标面板**：「任务目标」改为任务图标总开关，去掉三态与计数
- **界面细节**：监控页任务分页、手机端顶栏自适应、任务板分页重置
- **设置项改为即时生效**，去掉「保存」按钮
- **截图清理**：自动删除模式下顺带清理没有坐标的截图
