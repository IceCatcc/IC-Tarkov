## 新增功能

- **任务目标可单独勾选完成**：多目标任务（如「这是什么梗」27 个目标）不再只能整任务标记，每个目标都能单独打勾
  - 监控页任务卡片、任务板卡片、任务链详情面板均可勾选，详情面板显示「目标 n/m」
  - 地图上的任务目标弹窗也能勾选，勾选行就是「勾选框 + 目标描述」
  - 已勾选的目标在地图上图标转为半透明
  - 进度按 PVP / PVPS / PVE 分别保存，并随局域网同步到手机端
- **互斥任务（多选一）判定为已失败**：交了同组任务之一后，其余任务标记为「已失败」，不再出现在地图与进行中列表（如海关的化学品 - 4 / 大客户 / 出于好奇）
- **地图图标悬浮显示名称**：鼠标悬停（手机端点按）即可看到标记名称，弹窗不再显示坐标行
- **Boss 出生点显示 Boss 名称与刷新率**：直观看到各点位会刷什么 Boss、概率多少

## 优化与修复

- **钥匙锁显示钥匙名**：此前地图上的锁全部显示「未命名」，现在显示对应钥匙的中文名（如「工厂紧急出口钥匙」），同一把钥匙的几扇门归为一组
- **开关 / 拉杆改为中文名**：如「医疗区电梯电源按钮」「主电梯呼叫按钮」「警报开关」，共 32 条
- **同一目标不再重复绘制**：上游数据的 day / night 副本（坐标还可能略有出入）会导致一个目标画两个图标，已按 zone id 去重
- **同坐标的多个目标合并为一个图标**：如「找到物品」+「放置标记器」这类同点目标，弹窗内逐条列出、可分别勾选，不再重叠看不清
- **地图弹窗细节**：标题去掉「◎」追踪符号，勾选行不再加外框
