## 新增功能

- **任务列表关键字搜索**：筛选栏新增搜索框，可在已选分类内按任务名过滤，快速定位目标任务
- **地图狙击 AI 图层增强**：狙击手识别新增 `zoneName` 维度（`Snipe` / `Sniper`），覆盖海关、中心区、灯塔、海岸线等地图
- **地图「自动居中」选项**：可在设置中开关，仅开启时才显示聚焦缩放比例条

## 优化改进

- **地图标记图层设置持久化**：标记图层的显隐由组件本地状态迁移至 `store`（`mapChips`），随 `settings.json` 保存，跨启动保留
- **商人展示名统一**：一律显示为「中文名-英文名」（`traderDisplayName`），任务卡片、详情与筛选联动
- **地图页移除游戏内时钟**：去掉不再使用的实时时钟显示，界面更清爽

## 问题修复

- 修复 BTR 等无嵌套 `position` 的地图标记整条被跳过的问题：坐标解析回退扁平 `x` / `z`
- 修复地图 Transit 转移后仍停留在原地图的问题：转移链改取最后一个地图

## 其他

- 后端 `dataset` 透传 `zoneName`，`types.MarkerEntry` 新增该字段
- README 功能特性文案同步更新
