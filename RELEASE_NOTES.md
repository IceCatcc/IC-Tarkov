## 新增功能

- **Wiki 站点选择**：设置中可切换 Wiki 站点（eftarkov / TarkovBox / 自定义），自定义支持 `{taskid}` 占位模板
- **Wiki 抽屉宽度可拖动**：桌面端可拖动 Wiki 抽屉左边缘调整宽度，调整后自动保留
- **地图标记「图标」筛选**：标记入口改为「图标」，筛选升级为分类 / 子分类两级，每个子项可单独开关
- **手机端屏幕常亮开关**：移动端可在设置中开启屏幕常亮，查看任务时保持亮屏

## 优化与修复

- **界面缩放适配**：开启界面缩放后，浮层按缩放比例反算尺寸，避免溢出屏幕
- **地图选单优化**：任务选单加高、层级选单不再顶开聚焦项、自动缩放改为左对齐
- **移动端连接体验**：移动端首次启动不再弹出连接窗口；电脑端连接按钮实时显示「已连接」状态
- **入口整理**：帮助与关于入口统一收拢到设置页
