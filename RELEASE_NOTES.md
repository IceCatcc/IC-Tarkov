## 新增功能

- **收藏家页面**：顶部导航新增「收藏家」页，以卡片表展示收藏家任务所需的全部收集品
  - 点击卡片即可标记为已收集，卡片前覆盖灰色遮罩并叠加居中的绿色对勾，再次点击取消
  - 收集进度持久化保存到数据目录的 `collected.json`，随导出 / 导入数据一同备份
  - 卡片右下角显示浅白色圆圈对勾徽标，代表该物品必须「战局内拾取」（FIR）
  - 卡片网格按窗口宽高自动求解列数与卡宽，无论怎么调整窗口都尽量铺满
  - 底部紧凑显示前置任务完成进度与等级 / 商人好感要求（根据任务数据与档案设置判定）
- **安装向导支持中文**：NSIS 安装包内置简体中文与英文，按系统语言自动匹配，中文系统直接显示中文向导

## 优化改进

- **任务图谱布局规整化**
  - 任务分列由「按解锁等级」改为「按拓扑深度」，首列任务数由 287 降至 54，不再堆成一团
  - 孤立任务（既无前置也不作为任何任务的前置，约占一半）单独成区并置于最左，不再挤占链路列
  - 商人泳道与列内任务顺序改用重心排序，让有依赖的任务尽量对齐，连线交叉数由 285 降至 234（约 -18%）
  - 连线加粗提亮：未完成依赖 2.8px → 3.8px、颜色 `#5b6773` → `#aab6c2`，已完成依赖改细改暗以弱化存在感
  - 每个商人小列的行数上限由 3 调整为 5，图谱更紧凑
- **等级标注改到卡片上**：移除顶部等级标尺，改为在任务卡片右上角显示等级 chip
  - Lv1+ 不再显示（默认值、无信息量）
  - Lv15+ 显示为蓝色、Lv30+ 显示为琥珀色，门槛一眼可辨
- 已完成任务卡片的配色进一步弱化（更贴近画布底色、文字更暗）
- Boss 名前显示头像小图标
- 地图信息浮层支持鼠标穿透，不再遮挡地图拖动与文字选中
- 任务筛选中「旧任务」相关文案统一改为「赛季任务」，与实际含义一致

## 问题修复

- 修复数据更新全部失败的问题：ureq 2.x 需显式装配 native-tls 连接器，否则 HTTPS 请求报「no TLS backend is configured」
- 修复 Fence「亡羊补牢」等任务的好感判定错误：此前忽略了数据自带的 `compareMethod`，一律按「≥」判定，导致要求好感**小于**阈值的任务永远显示未达标；现按 `<` / `<=` 等正确比较，并同步修正展示文案（如「好感 < -1」）
- 修复「好感达标」筛选漏判商人忠诚等级要求，导致未达标的任务没有被正确隐藏
- 修复迁移数据目录后页面白屏、数据目录回退的问题：移除 `data_location.txt` 标记文件机制，改为按「数据实际在哪」自动探测（程序目录 `data` 优先 → AppData 兜底 → 两处都没有则在程序目录新建），位置选择记录在 `settings.json`
  - 设置页恢复「数据目录位置」选项，切换时会写入 `settings.json` 并把数据整体迁移到目标目录
  - 目标位置已存在另一份数据时拒绝迁移，避免两份数据相互覆盖
  - 缓存为空时不再卡在后台空抓，直接跳过同步并在设置页提示点击「更新数据」
- 修复保存界面偏好时会抹掉后端写入的 `settings.json` 字段（`ui_prefs` 改为合并写入）
- 修复「关于」弹窗被帮助窗口遮挡的问题（调整浮层层级）

## 其他

- 版本号统一由 `src-tauri/Cargo.toml` 单一维护
- 项目补充 MIT 开源许可证
