{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "EFT Spy 默认窗口能力（允许对话框等插件）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
